SUBDIRS = tgdb src
EXTRA_DIST = config

# Stop rebuilding if timestamps are off.

$(srcdir)/Makefile.in:
Makefile:
$(ACLOCAL_M4):
config.status:
$(srcdir)/configure:
config.h:
stamp-h:
$(srcdir)/config.h.in:
$(srcdir)/stamp-h.in:

dist-hook:
	rm -rf `find $(distdir)/config -name CVS`
